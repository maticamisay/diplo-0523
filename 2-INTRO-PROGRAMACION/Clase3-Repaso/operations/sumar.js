// palabra reservada + nombre + argumentos(opcional) + bloque a encapsular
function sumarDosValores(a, b) {
  return a + b;
}

// module.exports = nombre de la funcion
module.exports = sumarDosValores;
